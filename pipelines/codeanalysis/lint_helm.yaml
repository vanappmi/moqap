---
include:
  - project: "moqap/modules"
    ref: "4.0.7"
    file:
      - "/lint/helm/lint_helm.yml"

lint_helm:
  extends: .lint_helm
  stage: static-codeanalysis
  tags:
    - eks-dev
  variables:
    LINT_HELM_PATH: "./helm-charts/eks"
